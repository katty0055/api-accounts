apiVersion: v1
kind: Service
metadata:
  name: seq
  namespace: accounts
  labels:
    app: seq
spec:
  type: NodePort
  selector:
    app: seq
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30081
