apiVersion: v1
kind: Service
metadata:
  name: accounts-api
  namespace: accounts
spec:
  type: NodePort
  selector:
    app: accounts-api
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
