La API de cuentas fue desplegada correctamente. ??

Namespace: {{ .Release.Namespace }}
Release:   {{ .Release.Name }}

Para acceder a la API en Minikube:

  minikube service accounts-api -n {{ .Release.Namespace }} --url

O directamente (NodePort {{ .Values.api.service.nodePort }}):

  curl http://$(minikube ip):{{ .Values.api.service.nodePort }}/api/accounts

Ver el estado de los pods:

  kubectl get pods -n {{ .Release.Namespace }}
